# Cargo build artifacts — bench workspace + every isolated program sub-workspace.
target/

# Generated benchmark outputs. Regenerate with `cargo test --test multisig -- --nocapture`
# (plus `BENCH_FLAMEGRAPHS=1` for the SVGs).
flamegraphs/
graphs/
