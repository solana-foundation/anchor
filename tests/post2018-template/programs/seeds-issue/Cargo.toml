[package]
name = "seeds-issue"
version = "0.1.0"
description = "Test program for seeds constraint IDL generation issue"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "seeds_issue"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { path = "../../../../lang" }

