[package]
name = "multi_cpi_test"
version = "0.1.0"
description = "Test for multiple CPI reload validation"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
anchor-lang = { path = "../../../lang", features = ["init-if-needed"] }
anchor-spl = { path = "../../../spl" }

