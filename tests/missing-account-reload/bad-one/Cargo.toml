[package]
name = "bad_one"
version = "0.1.0"
description = "Bad example - missing reload after CPI"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
anchor-lang = { path = "../../../lang", features = ["init-if-needed"] }
anchor-spl = { path = "../../../spl" }

